// Shared IDs referenced across fixture files, kept separate from team.ts /
// players.ts to avoid a circular import (team.ts's legacy players[] array
// needs to reference players.ts's fixtures).

export const TEAM_ID = "TM551420";
